{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch program",
            "skipFiles": [
                "<node_internals>/**"
            ],
            // "program": "${file}",
            "program": "${workspaceFolder}/src/hello.ts",
             "preLaunchTask": "tsc: build - tsconfig.json",
            "outFiles": [
                "${workspaceFolder}/**/*.js"
            ]
        }
    ]
}